# MongoDB Connection
# Option 1: MongoDB Atlas (paste your connection string below)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/foodie

# Option 2: Local MongoDB
# MONGODB_URI=mongodb://localhost:27017/foodie

# If no MONGODB_URI is set, the app uses mongodb-memory-server (in-memory, no install needed)

# JWT Secret
JWT_SECRET=foodie_jwt_secret_key_2024_super_secure

# Server Port
PORT=3000
